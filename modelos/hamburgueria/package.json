{
  "name": "neoeffex-modelo-hamburgueria",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "live:sync": "node tools/sync-live.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:publish": "npm run build && node tools/publish-build.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "validate": "node tests/validate.mjs"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
