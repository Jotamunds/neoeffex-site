/* Paleta padrão. O botão Exportar cores gera um substituto para este arquivo. */
window.NEOEFFEX_THEME = Object.freeze({
    accent: "#a06a3b",
    background: "#ffffff",
    soft: "#faf8f4",
    dark: "#1c1b19"
});
